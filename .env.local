# Backend API base URL — update port to match your running backend
# Run the backend with: dotnet run --project src/DesignSystem.Api
# Then check which port is printed (e.g. "Now listening on: https://localhost:7001")
VITE_API_BASE_URL=http://127.0.0.1:5000
